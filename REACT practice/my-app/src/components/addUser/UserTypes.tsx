export interface UserTypes {
    id: number; // id является необязательным полем
    firstName: string;
    lastName: string;
    email: string;
    age: number;
    bio: string;
    isHappy: boolean;
    // Другие свойства, если они есть
}